- @overview.each do |month, posts|
  %div.blog_grouping
    = month
    %span.bracket
      (
      %span.blogNum> #{posts.count}
      )
    %div.blog_grouping_item
      - posts.each do |post|
        %div
          \- #{link_to post.title, post_path(post)}
