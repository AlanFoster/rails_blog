%h1 Listing posts
%table
  %thead
    %tr
      %th Title
      %th Content
      %th Tools
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= post.content[0..30] + '...'
        %td
          = link_to 'Show', post
          |
          = link_to 'Edit', edit_admin_post_path(post)
%br
= link_to 'New Post', new_admin_post_path
